# Iconos PWA

Coloca aquí `icon-192.png` y `icon-512.png` (referenciados por `public/manifest.json`).
Pendiente del sistema de diseño (§C-5.2 `brand.ts` no fija valores de marca). Sin estos
archivos la app funciona pero la instalación PWA no mostrará icono.
