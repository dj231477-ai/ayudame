// Stub de `server-only` para el entorno de test (vitest, node). El paquete real lanza al
// importarse fuera de un React Server Component; en tests solo necesitamos que sea un no-op.
// Aliased en apps/flowday/vitest.config.ts.
export {};
